# F1 Dashboard Proxy - Environment Variables

# Server port (optional, defaults to 4000)
PORT=4000

# Proxy mode: "f1dash" | "live-polling" | "mqtt" | "replay" | "auto"
# f1dash: Uses f1-dash.com's SSE endpoint (default, no credentials needed)
# live-polling: Polls OpenF1 API
# mqtt: Uses OpenF1 MQTT (requires credentials)
# replay: Uses recorded data for testing
# auto: Automatically selects based on available credentials
PROXY_MODE=f1dash

# F1 SSE API Configuration (for PROXY_MODE=f1dash)
# Configure the SSE endpoint for real-time F1 data
F1_SSE_HOST=living-f1-sse.example.com
F1_SSE_PATH=/api/realtime
F1_SSE_SECURE=true

# OpenF1 API Credentials for live data via MQTT
# Get credentials at: https://openf1.org/auth.html
# Without these, the proxy will use replay mode
OPENF1_USERNAME=your_email@example.com
OPENF1_PASSWORD=your_password
